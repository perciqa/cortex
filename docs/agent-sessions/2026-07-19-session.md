# Session: 2026-07-19

## Goal
Implement embedder-mocked e2e test for the Cortex fabric that runs in CI without PyTorch/transformers/hnswlib.

## Files Changed
- `tests/e2e/test_mocked_embedder_e2e.py` — new file with 3 e2e tests

## Tests Created
1. **test_mocked_seed_and_alpha** — seeds 10 articles, runs agent alpha (scripted reasoner), verifies insight, signatures, trust scores, query
2. **test_mocked_vllm_reasoner** — same flow with `--reasoner vllm` + mocked vLLM client, asserts prompt capture and synthetic response in insight body
3. **test_mocked_full_flow_with_beta** — seed → alpha insight → beta warning → provenance depth ≥ 2

## Key Discoveries
- `hnswlib` C extension segfaults on Python 3.14 (pre-existing CI issue). Both embedder AND vector index are mocked to avoid this.
- `FakeEmbedder` returns deterministic zero vectors; `FakeVectorIndex` implements brute-force cosine similarity in pure numpy.

## Commands Run
- `python -m pytest tests/e2e/test_mocked_embedder_e2e.py -v` (3/3 passed)
- `ruff check tests/e2e/test_mocked_embedder_e2e.py` (clean)
- `pip install --force-reinstall --no-binary hnswlib hnswlib` (attempted to fix segfault)
