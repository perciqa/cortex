# Session — 2026-07-18

## Goal
Execute ROCm spike (Task 0) — PRD §8.2 critical path gate. Verify bge-small-en-v1.5 embeddings runnable on Radeon via ROCm or CPU fallback, measure throughput, document fallback narrative.

## Files changed
- `.gitignore` — added patterns for venv, caches, build artifacts
- `pyproject.toml` — project scaffold with deps, tool configs
- `cortex/__init__.py` — package skeleton
- `cortex/core/__init__.py`, `cortex/node/__init__.py`, `cortex/broker/__init__.py`, `cortex/sdk/__init__.py`, `cortex/console/__init__.py`, `cortex/bench/__init__.py` — module skeletons
- `tests/__init__.py`, `tests/conftest.py`, `tests/unit/__init__.py`, `tests/integration/__init__.py`, `tests/e2e/__init__.py` — test scaffold
- `spikes/rocm_embed_spike.py` — Day 1 spike: loads bge-small-en-v1.5, runs embed, measures latency/throughput
- `spikes/README.md` — documented measured results and fallback narrative

## Commands run
```bash
git checkout -b agent/rocm-spike
python3 -m venv .venv
pip install -e ".[dev,cpu]"
pytest -q  # 0 tests, no errors
rocm-smi  # not found — no Radeon on dev machine
python -c "import torch; print(torch.cuda.is_available())"  # False
python spikes/rocm_embed_spike.py 16  # device=cpu, 65.35ms, 244.8 embeds/sec
git add && git commit -S
```

## Results
- **ROCm spike: PASS (CPU fallback)**
- Embedding pipeline works end-to-end: model loads, tokenizes, embeds, L2-normalizes
- CPU latency: 65.35ms for batch 16 (well under 200ms p95 target)
- CPU throughput: 244.8 embeds/sec (well over 30 target)
- Radeon GPU: approved access, not yet available on dev machine
- Fallback narrative documented for judging
